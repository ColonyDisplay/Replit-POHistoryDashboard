modules = ["python-3.12", "nodejs-20", "web"]

[nix]
channel = "stable-25_05"
packages = ["postgresql"]

[agent]
expertMode = true

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Start application"

[[workflows.workflow]]
name = "Start application"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "([ -d react-ui/dist ] || (npm ci --prefix react-ui --no-audit --no-fund && npm run build --prefix react-ui)) && uvicorn po_api:app --host 0.0.0.0 --port 5000 --reload"
waitForPort = 5000

[workflows.workflow.metadata]
outputType = "webview"

[userenv]

[userenv.shared]
ENABLE_SCRIPT_RUNNER = "0"

[postMerge]
path = "scripts/post-merge.sh"
timeoutMs = 240000

[deployment]
deploymentTarget = "autoscale"
run = ["python", "-m", "uvicorn", "po_api:app", "--host", "0.0.0.0", "--port", "5000"]
build = ["bash", "-c", "npm ci --prefix react-ui --no-audit --no-fund && npm run build --prefix react-ui"]
